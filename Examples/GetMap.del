// Cache the result of GetMap().

define globalvar map;

rule: "Get the current map."
{
	map = GetMap();
	
	// The result of GetMap() can be compared with the Map enum.
	if (map == Map.Eichenwalde)
	{
		// ...
	}
}