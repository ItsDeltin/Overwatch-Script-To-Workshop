// internal vars
usevar globalvar A;
usevar playervar A;

define playervar mapID;

rule: "Get map ID"
	Event.Ongoing_EachPlayer

    if (IsButtonHeld(EventPlayer(), Button.Interact))

	{
        mapID = Deltin.GetMapIDCom();
        SmallMessage(EventPlayer(), mapID);
	}