// internal vars
usevar globalvar Z;
usevar playervar Z;

define playervar teleportTo;

rule: "do whatever.",
	Event.Ongoing_EachPlayer
	{
        teleportTo = Vector(16.3, -30.2, 19);
        Teleport(EventPlayer(), teleportTo);
	}   