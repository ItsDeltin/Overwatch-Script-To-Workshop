// internal vars
usevar globalvar A;
usevar playervar B;

/* define playervar test; uncomment for free crash */

rule: "do whatever.",
	Event.Ongoing_EachPlayer
	{
        AbortIf(IsOnObjective() != false);
	}