// internal vars
usevar globalvar A;
usevar playervar A;

define playervar mapID;
define globalvar Locations L;

rule: "Setup"
    {
        Locations = [Vector(1, 2, 3), Vector(10, 20, 30), Vector(3, 2, 1), Vector(301, 3, 1)]
    }

rule: "Get map ID"
	Event.Ongoing_EachPlayer

    if (IsButtonHeld(EventPlayer(), Button.Interact))

	{
        mapID = Deltin.GetMapID();
	}