// internal vars
usevar globalvar A;
usevar playervar A;

define playervar mapID;
define playervar Locations L;

rule: "Setup"
    {
        Locations = [Vector(1, 2, 3), Vector(1, 2, 3), Vector(3, 2, 1)]
    }

rule: "Get map ID"
	Event.Ongoing_EachPlayer

    if (IsButtonHeld(EventPlayer(), Button.Interact))

	{
        mapID = Deltin.GetMapID();
	}