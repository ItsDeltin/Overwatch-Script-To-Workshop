rule: "Recursion test."
Event.OngoingPlayer
if (IsButtonHeld(EventPlayer(), Button.Interact))
{
    Test(123);
}

method Test(result) 
{
    if (returns4() == 4)
    {
        SmallMessage(EventPlayer(), <"hello <0>! good_luck?", EventPlayer()>);
    }
}

method returns4()
{
    return 4;
}