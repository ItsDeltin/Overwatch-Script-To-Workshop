usevar globalvar A;
usevar playervar A;

define globalvar temp C;

rule: "Resursion test."
if (IsButtonHeld(EventPlayer(), Button.Interact))
{
    Count(1);
}

method Count(x)
{
    SmallMessage(EventPlayer(), x);
    temp = x; 
    if (x < 10)
        Count(x + 1);
    //Wait(3);
    //SmallMessage(EventPlayer(), "downloaded!");
}