usevar globalvar A;
usevar playervar A;

rule: "Resurrect player then send message."
if (IsButtonHeld(EventPlayer(), Button.Interact))
{
    Count(1);
}

method Count(x)
{
    SmallMessage(EventPlayer(), x);
    if (x < 10)
        Count(x + 1);
} 