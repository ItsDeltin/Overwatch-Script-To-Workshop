usevar globalvar A;
usevar playervar A;

define playervar isTargetted;

rule: "Resurrect player then send message."
{
    define i = 0;
    for (;;)
    {
        SmallMessage(EventPlayer(), i);
    }

    for (player in AllPlayers())
    {
        SmallMessage(AllPlayers()[player], "hello!");
        AllPlayers()[player].isTargetted = true;
    }

    while (NumberOfPlayers() < 3)
    {
        SmallMessage(EventPlayer(), <"waiting form least <0> players", 3>); 
    }
}