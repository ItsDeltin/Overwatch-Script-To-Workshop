usevar globalvar A;
usevar playervar A;

rule: "Resurrect player then send message."
{
    Resurrect(EventPlayer());
    BigMessage(EventPlayer(), <"resurrected! try_again, good_luck <0>!", EventPlayer()>);
}