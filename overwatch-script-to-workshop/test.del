rule: "Recursion test."
Event.OngoingPlayer 
if (IsButtonHeld(EventPlayer(), Button.Interact))
{
    foreach (define myVar in AllPlayers())
    {
        Teleport(myVar, PositionOf(myVar) + Vector(0, 5, 0))
        SmallMessage(EventPlayer(), myVar);
        MinWait();
    }
}